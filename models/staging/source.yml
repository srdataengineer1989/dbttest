version: 2
sources:
    - name: rawsource
      database: "{{('SALES')}}"
      schema: rawdata
      tables:
        - name: raw_customerdata
        - name: raw_orders
        - name: raw_customers_snapshot
